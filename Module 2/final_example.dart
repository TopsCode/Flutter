void main() {
  final int age = 25; // 'age' is final, so it can't be changed once assigned.
  
  print(age); // Output: 25
  
  // Uncommenting the following line will cause an error:
  // age = 30; // Error: Can't assign to the final variable 'age'.
}
